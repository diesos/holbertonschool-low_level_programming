#include "main.h"
#include <stdio.h>

/**
 * times_table - print the multiply table
 * @void: void
 * Return: Always 0.
 */

int main(void)
{
	int i;
	int j;

	for (i = 0; i < 10; i++)
	{
			putchar('0' + i);
			if (i != 9)
			{
				putchar(',');
				putchar(' ');
				putchar(' ');
			}
			else
				putchar('\n');
	}
}
