#include "main.h"

/**
 * _abs - computes the abs value of an int
 * @int : integer to check the abs value of
 * Return: Always 0.
 */

int _abs(int)
{
	if (int > 0)
		printf("%d", int);
	if (int == 0)
		printf("0");
	if (int < 0)
	{
		int = int * -int;
		printf("%d", int);
}
